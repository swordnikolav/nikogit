#!/usr/bin/env bash

read -p "Nombre del fichero que quieres crear en ~/.local/bin: " fname

# 1. create or truncate
truncate -s 0 "$HOME/.local/bin/$fname"

# 2. permisos ejecución
chmod +x "$HOME/.local/bin/$fname"

# 3. abre nv
nvim "$HOME/.local/bin/$fname"

