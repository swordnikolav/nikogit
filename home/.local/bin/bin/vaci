#!/bin/sh

# Uso: script <archivo>
# Ej: script mi_script.py
# Trunca el archivo y lo abre con nv

[ $# -ne 1 ] && { echo "Uso: $0 <archivo>"; exit 1; }

archivo="$1"

# Truncar (vaciar) el archivo
> "$archivo"

# Abrir con neovim usando alias nv
nvim "$archivo"
